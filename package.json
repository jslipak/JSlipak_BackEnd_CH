{
  "name": "entregable7",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "export NODE_ENV=production && nodemon src/app.js",
    "dev": "export NODE_ENV=development && nodemon src/app.js",
    "mongo": "export NODE_ENV=mongo && nodemon src/app.js",
    "firebase": "export NODE_ENV=firebase && nodemon src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "express-handlebars": "^6.0.4",
    "mongoose": "^6.3.3",
    "morgan": "^1.10.0"
  }
}
